html
    head
        title Snomed release #{release} - V&auml;lj fråga
    body
        h1 V&auml;lj fråga

        ul
            for query in queries
                li
                    a(href='/releasecheck/query/' + query.id + '/' + release) #{query.description}

            li 
                a(href='/releasecheck/all-queries/' + release) Alla fr&aring;gor samtidigt
    
