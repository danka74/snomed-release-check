html
    head
        title Snomed release #{release} - V&auml;lj fråga
    body
        h1 V&auml;lj fråga

        for query in queries
            a(href='/query/' + query.path + '/' + release) #{query.path}
    